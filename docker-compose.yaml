services:
  migration:
    build: .
    env_file: .env
    command: >
      sh -c "sleep 30 && alembic upgrade head"
  bot:
    build: .
    container_name: aio_bot
    env_file: .env
    restart: unless-stopped


# volumes:
#   pg_data:
#   redis_data: